# SQL Developer Internship – Task 3  
### **Writing Basic SELECT Queries**

This task focuses on extracting data from tables using SQL `SELECT` queries.  
It includes filtering, sorting, limiting results, and using common SQL operators.

---

## ✅ **What This Task Covers**
- Using `SELECT` to retrieve data  
- Filtering rows with `WHERE`, `LIKE`, `BETWEEN`  
- Sorting results using `ORDER BY`  
- Limiting rows with `LIMIT`  
- Using `DISTINCT`, `IN`, and column aliasing  

---

## 📁 **Files Included**
- `task3.sql` — Contains all SELECT queries for the task  
- Screenshots (if required by the internship)  

---

## 🔍 **Queries Covered**
1. `SELECT *`
2. Selecting specific columns  
3. Filtering data with `WHERE`  
4. Pattern matching with `LIKE`  
5. Range filtering using `BETWEEN`  
6. Sorting using `ORDER BY`  
7. Limiting output with `LIMIT`  
8. Removing duplicates using `DISTINCT`  
9. Filtering using `IN`  
10. Aliasing columns  

---

## 🗃 **Database Used**
The queries are written on top of the schema created in **Task 1** and data inserted in **Task 2** (`ecommerce` database).

---

## 📤 Submission
Upload the GitHub repository link containing:
- `task3.sql`
- `README.md`
- Any screenshots if needed

---

If you want, I can also help create the folder structure + screenshot explanation for your repo.
